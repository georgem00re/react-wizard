import React from "react";
import "./index.css";

const App<%= (typescript === true || typescript === "true") ? ": React.FC" : "" %> = () => {
    return (
        <h1>React Application</h1>
    );
}

export default App;
